services:
  - type: web
    name: horizon-website
    runtime: static
    buildCommand: cd website && npm install && npm run build
    staticPublishPath: ./website/dist
    headers:
      - path: /*
        name: X-Frame-Options
        value: SAMEORIGIN
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
